# Axolotl Bucket Fix

Standalone split of Better Client's axolotl bucket fix for Minecraft 26.1+.

## Features

- Gives each axolotl bucket variant its own texture
- Registers the custom item model property on Fabric and NeoForge
- Keeps datagen and generated item models
